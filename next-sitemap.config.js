/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://saintark.my.id', // ← your site URL (important)
  generateRobotsTxt: true, // ← generates robots.txt too
  sitemapSize: 5000,
};
