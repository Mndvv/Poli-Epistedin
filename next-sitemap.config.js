/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://www.saintark.my.id', // ← your site URL (important)
  generateRobotsTxt: true, // ← generates robots.txt too
  sitemapSize: 5000,
};
